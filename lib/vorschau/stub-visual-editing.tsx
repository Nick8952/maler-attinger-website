/**
 * Ersatz für `next-sanity/visual-editing` im statischen Export (siehe next.config.ts).
 * Das Original bringt Server Actions mit, die es ohne Server nicht gibt.
 */
export function VisualEditing(): null {
  return null;
}
